<div class='container'>
  <!-- Content here -->
  {{#if displayTitle}}
    <h1>{{title}}</h1>
  {{else}}
    <h1>List users</h1>
  {{/if}}

  <!-- Table -->
  <div class='row'>
    <div class='col'>Name</div>
    <div class='col'>Adress</div>
    <div class='col'>Link</div>
  </div>
  {{#each users}}
    <div class='row'>

      <div class='col'>
        {{this.name}}
      </div>
      <div class='col'>
        Adrees
      </div>
      <div class='col'>
        {{this.website}}
      </div>
    </div>
  {{/each}}

</div>